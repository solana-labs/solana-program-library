/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'
import {
  GovernanceAccountType,
  governanceAccountTypeBeet,
} from '../types/GovernanceAccountType'
import { InstructionData, instructionDataBeet } from '../types/InstructionData'
import {
  TransactionExecutionStatus,
  transactionExecutionStatusBeet,
} from '../types/TransactionExecutionStatus'

/**
 * Arguments used to create {@link ProposalTransactionV2}
 * @category Accounts
 * @category generated
 */
export type ProposalTransactionV2Args = {
  accountType: GovernanceAccountType
  proposal: web3.PublicKey
  optionIndex: number
  transactionIndex: number
  holdUpTime: number
  instructions: InstructionData[]
  executedAt: beet.COption<beet.bignum>
  executionStatus: TransactionExecutionStatus
  reservedV2: number[] /* size: 8 */
}
/**
 * Holds the data for the {@link ProposalTransactionV2} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class ProposalTransactionV2 implements ProposalTransactionV2Args {
  private constructor(
    readonly accountType: GovernanceAccountType,
    readonly proposal: web3.PublicKey,
    readonly optionIndex: number,
    readonly transactionIndex: number,
    readonly holdUpTime: number,
    readonly instructions: InstructionData[],
    readonly executedAt: beet.COption<beet.bignum>,
    readonly executionStatus: TransactionExecutionStatus,
    readonly reservedV2: number[] /* size: 8 */
  ) {}

  /**
   * Creates a {@link ProposalTransactionV2} instance from the provided args.
   */
  static fromArgs(args: ProposalTransactionV2Args) {
    return new ProposalTransactionV2(
      args.accountType,
      args.proposal,
      args.optionIndex,
      args.transactionIndex,
      args.holdUpTime,
      args.instructions,
      args.executedAt,
      args.executionStatus,
      args.reservedV2
    )
  }

  /**
   * Deserializes the {@link ProposalTransactionV2} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [ProposalTransactionV2, number] {
    return ProposalTransactionV2.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link ProposalTransactionV2} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey
  ): Promise<ProposalTransactionV2> {
    const accountInfo = await connection.getAccountInfo(address)
    if (accountInfo == null) {
      throw new Error(
        `Unable to find ProposalTransactionV2 account at ${address}`
      )
    }
    return ProposalTransactionV2.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'GovER5Lthms3bLBqWub97yVrMmEogzX7xNjdXpPPCVZw'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(
      programId,
      proposalTransactionV2Beet
    )
  }

  /**
   * Deserializes the {@link ProposalTransactionV2} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [ProposalTransactionV2, number] {
    return proposalTransactionV2Beet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link ProposalTransactionV2} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return proposalTransactionV2Beet.serialize(this)
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link ProposalTransactionV2} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args: ProposalTransactionV2Args) {
    const instance = ProposalTransactionV2.fromArgs(args)
    return proposalTransactionV2Beet.toFixedFromValue(instance).byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link ProposalTransactionV2} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    args: ProposalTransactionV2Args,
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      ProposalTransactionV2.byteSize(args),
      commitment
    )
  }

  /**
   * Returns a readable version of {@link ProposalTransactionV2} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      accountType:
        'GovernanceAccountType.' + GovernanceAccountType[this.accountType],
      proposal: this.proposal.toBase58(),
      optionIndex: this.optionIndex,
      transactionIndex: this.transactionIndex,
      holdUpTime: this.holdUpTime,
      instructions: this.instructions,
      executedAt: this.executedAt,
      executionStatus:
        'TransactionExecutionStatus.' +
        TransactionExecutionStatus[this.executionStatus],
      reservedV2: this.reservedV2,
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const proposalTransactionV2Beet = new beet.FixableBeetStruct<
  ProposalTransactionV2,
  ProposalTransactionV2Args
>(
  [
    ['accountType', governanceAccountTypeBeet],
    ['proposal', beetSolana.publicKey],
    ['optionIndex', beet.u8],
    ['transactionIndex', beet.u16],
    ['holdUpTime', beet.u32],
    ['instructions', beet.array(instructionDataBeet)],
    ['executedAt', beet.coption(beet.i64)],
    ['executionStatus', transactionExecutionStatusBeet],
    ['reservedV2', beet.uniformFixedSizeArray(beet.u8, 8)],
  ],
  ProposalTransactionV2.fromArgs,
  'ProposalTransactionV2'
)
