/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as beet from '@metaplex-foundation/beet'
import * as web3 from '@solana/web3.js'
import * as beetSolana from '@metaplex-foundation/beet-solana'
import {
  GovernanceAccountType,
  governanceAccountTypeBeet,
} from '../types/GovernanceAccountType'

/**
 * Arguments used to create {@link ProgramMetadata}
 * @category Accounts
 * @category generated
 */
export type ProgramMetadataArgs = {
  accountType: GovernanceAccountType
  updatedAt: beet.bignum
  version: string
  reserved: number[] /* size: 64 */
}
/**
 * Holds the data for the {@link ProgramMetadata} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class ProgramMetadata implements ProgramMetadataArgs {
  private constructor(
    readonly accountType: GovernanceAccountType,
    readonly updatedAt: beet.bignum,
    readonly version: string,
    readonly reserved: number[] /* size: 64 */
  ) {}

  /**
   * Creates a {@link ProgramMetadata} instance from the provided args.
   */
  static fromArgs(args: ProgramMetadataArgs) {
    return new ProgramMetadata(
      args.accountType,
      args.updatedAt,
      args.version,
      args.reserved
    )
  }

  /**
   * Deserializes the {@link ProgramMetadata} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [ProgramMetadata, number] {
    return ProgramMetadata.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link ProgramMetadata} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey
  ): Promise<ProgramMetadata> {
    const accountInfo = await connection.getAccountInfo(address)
    if (accountInfo == null) {
      throw new Error(`Unable to find ProgramMetadata account at ${address}`)
    }
    return ProgramMetadata.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'GovER5Lthms3bLBqWub97yVrMmEogzX7xNjdXpPPCVZw'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, programMetadataBeet)
  }

  /**
   * Deserializes the {@link ProgramMetadata} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [ProgramMetadata, number] {
    return programMetadataBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link ProgramMetadata} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return programMetadataBeet.serialize(this)
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link ProgramMetadata} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args: ProgramMetadataArgs) {
    const instance = ProgramMetadata.fromArgs(args)
    return programMetadataBeet.toFixedFromValue(instance).byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link ProgramMetadata} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    args: ProgramMetadataArgs,
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      ProgramMetadata.byteSize(args),
      commitment
    )
  }

  /**
   * Returns a readable version of {@link ProgramMetadata} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      accountType:
        'GovernanceAccountType.' + GovernanceAccountType[this.accountType],
      updatedAt: this.updatedAt,
      version: this.version,
      reserved: this.reserved,
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const programMetadataBeet = new beet.FixableBeetStruct<
  ProgramMetadata,
  ProgramMetadataArgs
>(
  [
    ['accountType', governanceAccountTypeBeet],
    ['updatedAt', beet.u64],
    ['version', beet.utf8String],
    ['reserved', beet.uniformFixedSizeArray(beet.u8, 64)],
  ],
  ProgramMetadata.fromArgs,
  'ProgramMetadata'
)
